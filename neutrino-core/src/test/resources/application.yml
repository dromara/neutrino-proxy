neutrino:
  application:
    name: neutrino-proxy-server
  http:
    enable: false
    port: 8080
    context-path: /test
    max-content-length-desc: 128K
    static-resource:
      locations:
        - "classpath:/static/"
        - "classpath:/template/"
