neutrino:
  application:
    name:
  http:
    enable: false
    port:
    context-path: /test
    max-content-length-desc: 128K
    static-resource:
      locations:
        - "classpath:/static/"
        - "classpath:/template/"
